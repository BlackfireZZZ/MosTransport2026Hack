# Technical debt register

Record only a concrete compromise with a demonstrable effect and a closure condition. “Improve the code” is not a task.

| ID | Area | Observable risk/cost | Evidence | Closure condition | Priority | Owner |
|---|---|---|---|---|---|---|
| TD-001 | ml/features | The three recorded feature digests live only in `ml/README.md` and the completed plan; no test pins them, so a future change can move them silently and the documented determinism evidence goes stale without any check failing | The lead reproduced `7e412bb0…`, `b3ad914c…`, `408d4580…` by hand on 2026-09-25; `grep` finds them in documentation only, never in `ml/tests` | A test asserts the three digests for the committed fixture, or the digests are removed from the documentation in favour of the relative determinism assertions that already exist | P2 | TASK-058 |
| TD-002 | backend/db | `stops.name` carries `UniqueConstraint("name")`, but real stop names are not globally unique; seeding real stops would reject rows, and any name-keyed join would merge distinct places | The committed OSM extract has 382 names on more than one node (349 on two, 19 on four, 11 on three, one each on five, six and ten); the synthetic catalog gives two different stops the name "Тестовая площадь" deliberately | The constraint is dropped, or replaced by one that matches the real key, before the first real stop rows are seeded | P2 | TASK-032 |
| TD-003 | ml/route publication | Real route-hour model is offline: no production route-ID mapping, atomic publisher, uncertainty or observed delivery coverage | TASK-060 report/verification explicitly covers schema projection only; route5 unsupported, source availability unknown | Version route-only publication, resolve organizer route numbers to catalog IDs, test DB/API/UI roundtrip; calibrate intervals on temporal slices and add availability evidence before production quality claims | P1 | ML/publication lead |
| TD-004 | ml/boarding timetable potentials | Unequal unnormalized exact/proxy precision biases phase support; route7 July29 has10,382 affected payments and17 raw strict candidates, all excluded from training | [Controlled fixture and real-source diagnosis](../analysis/2026-09-26-real-stops/model-quality.json); exporter guard is specific to the frozen2025v1 experiment | Use comparable normalized clock likelihoods or a justified common-precision model, test identical-source clocks for zero source-only phase preference, version and rerun affected inference before removing the explicit guard | P1 | ML/boarding lead |
| TD-005 | ml/boarding target validity | All stop targets remain conditional inference; candidate assignments are sensitive to burst changes, current geometry/timetables are transferred, and zero strict labels passed final eligibility | [Full real-data evidence](../analysis/2026-09-26-real-stops/README.md), six-session perturbations, incomplete official service calendar | Obtain independent stop truth or an independently validated equivalent, compare baseline/error and calibration across temporal slices, resolve dated service/geometry coverage before claiming measured stop forecasting accuracy or publishing these targets as observations | P1 | ML/boarding lead |
