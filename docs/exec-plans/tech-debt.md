# Technical debt register

Record only a concrete compromise with a demonstrable effect and a closure condition. “Improve the code” is not a task.

| ID | Area | Observable risk/cost | Evidence | Closure condition | Priority | Owner |
|---|---|---|---|---|---|---|
| ~~TD-001~~ | ml/features | *Closed 2026-09-25.* The three feature digests were documented but pinned by no test | The gap was real: the TASK-020 review found an unclipped `*_units` column, the `year/month` digest moved from `408d4580…` to `58e57f49…`, and nothing failed | Closed by `test_the_published_digest_still_describes_the_committed_fixture` in `ml/tests/test_features_fixtures.py`, which pins all three digests, both feature digests and the row/column shapes | P2 | — |
| TD-002 | backend/db | `stops.name` carries `UniqueConstraint("name")`, but real stop names are not globally unique; seeding real stops would reject rows, and any name-keyed join would merge distinct places | The committed OSM extract has 382 names on more than one node (349 on two, 19 on four, 11 on three, one each on five, six and ten); the synthetic catalog gives two different stops the name "Тестовая площадь" deliberately | The constraint is dropped, or replaced by one that matches the real key, before the first real stop rows are seeded | P2 | TASK-032 |
| TD-003 | ml/intake | A headerless CSV whose every row-one cell is word-like still has those cells printed as column names, so data can reach the refusal text and the report; the line rule narrows the class sharply but cannot close it without a structural signal | Lead probe 2026-09-25: a headerless file of `ZZSURNAMEZZIvanov,ZZSURNAMEZZPetrov,ZZSURNAMEZZSidorov` prints all three, while a single punctuated, spaced or digit-leading cell anywhere condemns the whole line; comparing row-one shapes against row-two was tried and false-positives on real headers (`stop_id` and `synthetic:stop:1` share a signature) | An explicit `header: [...]` or per-stream column declaration in the profile lets an operator state the layout, or a corroborating structural signal is found that does not false-positive on real headers | P2 | TASK-049 |
