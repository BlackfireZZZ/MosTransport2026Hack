# TASK-060: real route forecasting experiments

Owner: route-ml-experiments lead. Base 6e2aabc222d34610b72e30e86ea536240872d62a;
worktree /Users/cute/MosTransport2026Hack-worktrees/route-ml-experiments;
branch agent/route-ml-experiments. Primary checkout remains read-only until integration.

## Outcome and scope

User asks for materially better ML after EDA, including CatBoost and feature ablations,
while preserving backend/frontend. Deliver reproducible offline experiments, best
trained local artifact, submission and a tested compatibility seam. No DB writes,
no frontend/backend production edits, no takeover of TASK-020/039, no external submission.
Model binaries belong in ignored ml/artifacts/; code/config/metrics remain versioned.

## Hypothesis and acceptance

Origin-frozen route history, calendar and horizon features improve WAPE over seasonal
baselines. Before fitting, compare fixed variants on 61-day development windows starting
2025-04-01, 2025-06-01, 2025-08-01. These overlap and are not independent samples.
Select using aggregate score with per-origin and per-route diagnostics. Freeze choice
before evaluating 2025-09-01 through 2025-10-31, reporting only October
(indices30:61) as post-selection confirmation: August development already uses September (previous EDA has already
exposed this period, so not an untouched blind holdout). No early stopping on evaluated
windows. Target objective: +0.01 absolute score on each development origin vs last-week
baseline; if not met, do not claim promotion and retain measured best challenger.
Check 1-day/31-day/61-day slices and record worst routes rather than hide regressions.

Inputs: organizer labels audited in TASK-059, complete 10-route hourly grid with explicit
zero-in-extract policy (not certified operating/source coverage). Route5 zero prior is
unsupported cold-start, never invent positive labels. Exclude all November source tail.
Time: Europe/Moscow, only event history before origin. Late delivery is unknown;
experiments assume retrospective availability and do not establish online readiness.

## Boundaries and evidence

Source target/grid: archive README and docs/analysis/2026-09-25-organizer-data.
Existing public forecast.v1 requires stops/directions, incompatible with real route input.
Do not weaken it. Offline route artifact needs its own version; bounded projections can
be checked against backend domain/HTTP schemas without importing training in workers.
Consumers inspected: ML CLI/features/evaluation, ForecastModel protocol (not wired),
ForecastService/repository, backend schemas, frontend API types. Existing app remains intact.

Smallest falsifiers: mutate post-origin labels and require identical features/predictions;
reject malformed/duplicate keys, unsafe dates/nonfinite values; serialized model reload
prediction equality; raw prediction→route artifact→day/month API projection value parity.
Final gates: focused ML/compatibility tests, Ruff/mypy, make check, independent review.

## Progress

- Read task/tracker/root and ML rules, README/DESIGN/architecture, prior EDA.
- Created isolated worktree. Existing untracked ZIP/PDF/.DS_Store left untouched.
- Read-only subagents assigned dependency research and application compatibility review.
- Optional CatBoost1.2.10 installed after the exact audit below; lock generated by uv.

## Research

https://catboost.ai/docs/en/references/training-parameters/common : has_time, categorical
processing and reproducible seed/thread controls. Dataset is small aggregated tabular
history, not 62M training observations. CatBoost is a candidate, not assumed superiority.
https://catboost.ai/docs/en/concepts/loss-functions-regression : MAE minimizes absolute
error, matching fixed-denominator WAPE; target-normalized variants need scale weights.
https://otexts.com/fpp3/tscv.html : frozen multi-step origins; no shuffled validation.

## Recovery and verification

No online switch occurs. Revert task commit to remove code/config without changing DB.
Local model artifacts retain manifest, source digest, config and model hashes. A failed
quality gate is explicit in metrics; saving an experimental candidate is not production
promotion. Update this plan and tracker with observed commands/results before handoff.

### Dependency evidence before installation (2026-09-25)

CatBoost1.2.10 explicitly requested comparator: ordered category statistics and tree
regularization differ from installed sklearn; existing sklearn remains a comparator.
Add only optional `boosting` extra, never backend/browser dependency. Exact dry-run
additions: catboost1.2.10 Apache2; contourpy1.4.0 BSD3; cycler0.12.1 BSD3;
fonttools4.66.0 MIT; graphviz0.21 MIT; kiwisolver1.5.1 BSD3; matplotlib3.11.2
PSF-derived Matplotlib licence with third-party notices; pillow12.3.0 MIT-CMU;
plotly7.1.0 MIT; pyparsing3.3.3 MIT. All ten exact versions: PyPI vulnerabilities=[]
and OSV query has no records. Audited via https://pypi.org/pypi/{name}/{version}/json
and POST https://api.osv.dev/v1/query before install. Licence source for CatBoost:
https://github.com/catboost/catboost/blob/master/LICENSE .

Linux x86_64 CPython3.13 summed wheels:131,018,087 bytes compressed,402,987,955 bytes
unpacked (384.32MiB); approximate offline environment/image content excluding bytecode,
filesystem/layer overhead. CatBoost alone287,594,940 bytes unpacked (mac98,921,024).
Backend image/browser bundle increase0 because neither installs the optional ML extra.

Calendar research: https://government.ru/docs/52895/ and /docs/all/155500/,
Resolution1335 dated2024-10-04; dates known before every experiment origin. HTTP body
fetch timed out, indexed official text confirms May2/8, June13, Nov3 transfers and
Nov1 working Saturday; Dec31 transfer also confirmed by official ministry source
https://mtsz.tatarstan.ru/index.htm/news/2480894.htm . No paid/calendar dependency.

### Review corrections before full comparison

Independent review identified September overlap with August development. October
only will be reported as post-selection confirmation from the frozen September1
origin; full61-day score is diagnostic and September reused. Previous EDA exposure
of October baseline remains disclosed. Optional CatBoost metadata handles absent
extra without breaking baseline/HGB. Artifact fields frozen, tuple points, strict
integer route IDs and metadata lengths<=128 prevent export mutation/schema drift.

## Completion evidence

24 candidates × 3 development origins completed; frozen cat_native selected with pooled
score .840430 and all per-origin gains over week1>=.01. October confirmation score
.866016 vs .813883; June remains worse than four-week mean. Results/config/source
hashes recorded in docs/analysis/2026-09-25-route-ml. Full labels fit saved14,640
November–December predictions; train reload equality and standalone predict equality pass.

Parent make check: exit0; architecture, Ruff, mypy,350backend passed/48SQL skipped,
299ML,92frontend,119reference passed; frontend build/OpenAPI/Compose checks passed.
Initial gate found import formatting I001 in a new test; fixed without changing checks.
Independent final review found no blockers. Actual artifact verification:30 domain/HTTP
views,10routes, exact14,640 template keys and prediction parity passed. No SQL, live UI,
E2E or production publisher verification was claimed; backend/frontend files unchanged.

Integration: local commit and fast-forward into main from recorded base, no push.
Worktree retained because branch is unpushed and ignored trained artifacts live there.
Next: resolve real catalog IDs and version/implement route-only atomic publication;
uncertainty and live source availability remain open (TD-003).
